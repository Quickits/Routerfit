<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.quickits.routerfit">

    <application>

        <provider
            android:name=".ARouterInitProvider"
            android:authorities="${applicationId}.routerfit"
            android:exported="false" />

    </application>

</manifest>
